import { Fork } from '@dbi/design-system';

export const Gold = () => (
  <Fork>
    Want the full suite — every function of a business, the Case Competition, and a
    company of your own on the Summit stage? <a href="#"><strong>That's the Flagship
    Program.</strong></a> This page is the smaller start: finance only.
  </Fork>
);

export const Red = () => (
  <Fork tone="red">
    Rather start smaller, in one lane? <a href="#">The Finance Program is a 25-seat,
    finance-only offshoot.</a> For everyone else — <strong>this is the program.</strong>
  </Fork>
);
