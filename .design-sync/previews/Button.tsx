import { Button } from '@dbi/design-system';

export const Primary = () => <Button variant="primary" href="#">Apply to DBI</Button>;
export const Gold = () => <Button variant="gold" href="#">Apply to the Finance Program</Button>;
export const Outline = () => <Button href="#">See what a session is like</Button>;
