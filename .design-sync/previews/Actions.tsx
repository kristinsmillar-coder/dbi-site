import { Actions, Button } from '@dbi/design-system';

export const Pair = () => (
  <Actions>
    <Button variant="primary" href="#">Apply to DBI</Button>
    <Button href="#">See what a session is like</Button>
  </Actions>
);
